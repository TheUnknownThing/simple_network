[package]
name = "sn-client"
version = "0.1.0"
edition = { workspace = true }
license = { workspace = true }

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
uuid = "1"

sn-proto = { path = "../sn-proto" }

# Linux TUN/TAP
# Keep this dependency isolated in client only.
tun = "0.6"
