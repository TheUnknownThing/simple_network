[workspace]
resolver = "2"
members = [
  "crates/sn-proto",
  "crates/sn-relay",
  "crates/sn-client",
]

[workspace.package]
edition = "2021"
license = "MIT OR Apache-2.0"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
strip = true
